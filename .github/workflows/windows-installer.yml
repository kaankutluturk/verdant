name: Build Windows installer

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    env:
      GITHUB_REF_NAME: ${{ github.ref_name }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          architecture: 'x64'

      - name: Build app and updater
        shell: cmd
        working-directory: ${{ github.workspace }}
        run: build_windows.bat

      - name: Install Inno Setup
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.4

      - name: Compile stable installer with ISCC
        shell: cmd
        working-directory: installer
        run: iscc verdant.iss

      - name: Compile demo installer with ISCC
        shell: cmd
        working-directory: installer
        run: iscc verdant_demo.iss

      - name: Upload installer artifacts
        uses: actions/upload-artifact@v4
        with:
          name: verdant-installers
          path: |
            installer/Output/verdant-setup.exe
            installer/Output/verdant-demo-setup.exe

      - name: Create GitHub Release and upload installers
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            installer/Output/verdant-setup.exe
            installer/Output/verdant-demo-setup.exe
          generate_release_notes: true
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }} 